package org.satsang.security;

//import org.jivesoftware.util.Blowfish;

public class SecurityManager {
	/*
	 * private String myEncryptionKey;
	 * 
	 * public SecurityManager() throws Exception { //TODO: Get the encryption
	 * key from keystore myEncryptionKey = "ShreeGurudev"; }
	 * 
	 * public String encrypt(String strEncrypt) { Blowfish bf = null; String
	 * encrypted = ""; try { bf = new Blowfish(this.myEncryptionKey); encrypted
	 * = bf.encryptString(strEncrypt); } catch (Exception ex) { strEncrypt = "";
	 * } finally { bf.destroy(); } return encrypted; }
	 * 
	 * public String decrypt(String strDeccrypt) { Blowfish bf = null; String
	 * decrypted = ""; try {
	 * 
	 * bf = new Blowfish(this.myEncryptionKey); decrypted =
	 * bf.decryptString(strDeccrypt); } catch (Exception ex) { strDeccrypt = "";
	 * } finally { bf.destroy(); } return decrypted; }
	 */
}
